# Catálogo de operações (exemplo)

Receitas **validadas**. Se o próximo agente precisa copiar o comando do transcript, a autoalimentação falhou.

Copie para `catalogo.md` (esse nome está no `.gitignore` do kit de propósito — o catálogo *real* da sua máquina não deve ir para um git público).

Formato: contexto → comando canônico → armadilha.

## Exemplo

### C-01 — (descreva a operação)

```bash
# comando que funcionou nesta máquina
```

- Armadilha: o que quebrou da primeira vez e como evitar.
